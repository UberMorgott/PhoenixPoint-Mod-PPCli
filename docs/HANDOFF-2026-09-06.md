# PPCLI handoff — session of 2026-09-05/06

Session role: working ON PPCLI (not as a consumer). Mode = drain `ISSUES.md`, the bug inbox other
sessions append to, fix each entry, verify live, commit, push, delete the entry.

## Where things stand

- Branch `main`, everything pushed to `UberMorgott/PhoenixPoint-Mod-PPCli`. Head at end of session:
  see `git log -1` (the session's own commits are the six below).
- `ISSUES.md` carries **two open entries**, both logged 2026-09-05 by a consumer session, both
  untouched by this session — that is the next session's queue.
- Client tests green throughout: `Invoke-Pester -Path tests` → paths 30, resolve-names 22, waits 15,
  index 6 assertions, plans 17.

## Shipped this session (each verified live, then committed and pushed)

| Commit | What | Verified by |
|---|---|---|
| `fa9a15f` | A recycled PID no longer wins the endpoint pick — an endpoint counts as live only if its pid is still `PhoenixPointWin64` for that install (executable path), newest game wins a tie | planted a fake endpoint carrying explorer.exe's pid → `swept stale endpoint`, real game answered |
| `2966072` | An evicted handle says so instead of blaming the TTL (`Reflect` counts evictions + highest evicted id) | 560 `call new` rows in one `multi` → `handle 'h:1:1' was EVICTED under pressure…512 leases…48 dropped` |
| `bfbcbc1` | A cold `batch` over 256 jobs is refused instead of silently truncated (bridge `Protocol.MaxJobs` drops the rest and still reports ok) | 561-row file → refusal naming the cap, exit 1 |
| `d0dbc3a` | Plan var refs NEST (`${ROWS.items[${i}].h}`, 8 passes max) and a real `null` stops looking like a typo (own refusal for args, JSON `null` in `output`) | one plan → `nested:"second"`, `realNull:null`, `missing:"unresolved: ${NOPE} is not set"` |
| `d0413d8` | `$box` envelope (box a primitive as a named type for an `Object` parameter); `screenshot` refuses `timeScale 0` under D3D12 instead of wedging the process; `screenshot` writes `*.scene.png` from `Camera.main.targetTexture` when an upscaler renders there | `Object.Equals($box Single, $box Single)`→true / vs `$box Double`→false; refusal live under `-force-d3d12`; scene PNG 853×480 with the menu Earth in it |
| `f5878b7` | `connect screenshot -Window` — client-side `PrintWindow(PW_RENDERFULLCONTENT)` grab of the game window AFTER present: the finished frame incl. upscaler blit and post-upscale passes, sized from `DWMWA_EXTENDED_FRAME_BOUNDS` (the game is DPI-unaware) | live grab on Instance3 with Renderforge DLSS on → 1262×712 upscaled scene + UI |

## Open — start here next session

Both entries are in `ISSUES.md`, evidence included there.

1. **`connect call` / `inspect` refuse a JSON argument built from a PowerShell expression**
   (`{"ok":false,"error":"Cannot find drive. A drive with the name '{\"status\"' does not exist."}`).
   Severity medium — blocks chaining on a handle from the shell; the workaround is a plan file.
   *Triage hint, unverified:* the quoted text is the start of ppcli's OWN reply, so suspect a
   `Test-Path`/`Split-Path`/`-Path` call in `ppcli.ps1` receiving output where it expects a path —
   most likely in the `@file` / `-` argument handling of the `connect` branch (an expression-built
   string starting with `{` should never be treated as a path, and is not, when quoted literally).
   Reproduce first: the reporter's exact three-step sequence, then bisect the client.
2. **`call {"op":"new"}` ignores `sig`**, so `ArrayList()` vs `ArrayList(int)` ties and cannot be
   selected; the refusal asks for a `sig` that is already there. Severity low. `New()` in
   `src/Reflect.cs` calls `Pick(ctors, args, null, ".ctor", out bound)` — the third argument is where
   `invoke` passes its `sig`; wiring `a["sig"]` through `Call()` into `New()` is the likely one-liner.

## Working notes worth keeping

- **Instances are contested.** `D:\PP-Instance2` is used by ContentTool/Renderforge sessions all day;
  do not fight for it. **`D:\PP-Instance3` is now a full automation install** — PPBridge deployed and
  ARMED (`Mods\PPBridge\ppcli-enabled` created this session), profile `76561197996210593` already has
  the mod activated. Drive it with `-PPRoot 'D:\PP-Instance3' -ProfileId 76561197996210593`.
- **For a multi-request live session, launch the game yourself** (`Start-Process
  D:\PP-Instance3\PhoenixPointWin64.exe -ArgumentList '-mods'`, endpoint file appears in ~3-10 s under
  `%LOCALAPPDATA%\ppcli\endpoints`) and use `connect`. `run`/`batch` STOP the game they started, so
  they are useless for a sequence that needs the game to stay up.
- `-force-d3d12` as a launch argument is how the D3D12 paths get tested.
- Peer sessions report defects and expect a reply: `contenttool-*` and `renderforge-*`. The
  `SendMessage` tool was unavailable late in the session; `mcp__ccd_session_mgmt__send_message` with a
  `sessionId` from `list_sessions` worked and is the fallback.
- The bridge build stamp changes on every deploy; `connect state` prints it, and `stale:true` in a
  cold run means the loaded DLL is not the deployed one — every number in that run is a ghost.
